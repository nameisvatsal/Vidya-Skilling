
const OfflineNotification = () => {
  return (
    <div className="bg-vidya-warning text-white text-center py-2 px-4 text-sm z-50 sticky top-0">
      You are currently offline. Your data will be synchronized when you reconnect.
    </div>
  );
};

export default OfflineNotification;
